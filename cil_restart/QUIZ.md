# CIL 自测题

1. 写出温度为 `T` 的旧类 KD，并证明给全部旧类 logits 加常数 `c` 时 KD 不变。
2. 仅用新类样本做 12 类 CE 时，为什么旧类输出行会被共同压低？写出梯度符号。
3. 区分 current accuracy、AA、AIA 和 forgetting；给出各自回答的研究问题。
4. B50-5S 一共有几个 task？每个 task 结束后学过多少类？
5. 为什么用测试准确率选 epoch 是数据泄漏？合理替代是什么？
6. 10 类和 11 类历史 checkpoint 为什么不是同一阶段的多样化 MTD 教师？
7. `均值提升 0.4，三次运行标准差 0.8` 能否称为有效提升？为什么？
8. 解释稳定性—可塑性散点图的右上角、左上角和右下角。
9. 解释为什么单新类增量不能直接照搬“新类内部 softmax”，本项目 D4 如何处理？
10. 主表终点与增量曲线终点不一致时，第一步应查什么？

